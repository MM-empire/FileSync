# File Sync for terminal

## Command list

```
filesync [-h | --help] [--sync-list=<path>] [--config=<path>]
    add <path>
    delete <path>
    diff <path>
    sync [--all] [-C | --current-dir]
    list [-C | --current-dir]
```
